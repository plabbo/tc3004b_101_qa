{
  "name": "calculator-app",
  "version": "1.0.0",
  "description": "Simple web calculator",
  "scripts": {
    "test": "cd tests && mocha *.js"
  },
  "dependencies": {
    "mocha": "^10.2.0",
    "selenium-webdriver": "^4.15.0"
  },
  "devDependencies": {
    "chromedriver": "^latest"
  }
}